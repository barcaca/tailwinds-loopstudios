// Importing components related to the gallery section
import { GalleryContent } from "./GalleryContent";
import { GalleryOverlay } from "./GalleryOverlay";
import { GalleryRoot } from "./GalleryRoot";

// Exporting an object containing references to gallery components
export const Gallery = {
  // Reference to the GalleryRoot component
  Root: GalleryRoot,
  // Reference to the GalleryOverlay component
  Overlay: GalleryOverlay,
  // Reference to the GalleryContent component
  Content: GalleryContent,
};